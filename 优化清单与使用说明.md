# 优化清单与使用说明

## 一、已完成优化清单

### 1. 运行环境与依赖

- 新增 `requirements.txt`，集中声明项目依赖。
- 依赖包括 Streamlit、NumPy、Pandas、SciPy、MNE、Scikit-Learn、Joblib、Matplotlib。
- 解决评审或换机运行时“不知道要安装什么包”的问题。

### 2. 路径与部署

- 去掉原来的本机绝对路径硬编码。
- 数据文件优先使用项目根目录下的 `BCICIV_2a_all_patients.csv`。
- 模型、特征缓存、报告统一输出到项目目录：
  - `models/<mode>/`
  - `artifacts/`
  - `reports/`
- 避免换电脑后因为路径不同导致程序崩溃。

### 3. 训练与演示解耦

- 新增 `train.py`，专门负责训练模型、保存模型、生成特征缓存和评估报告。
- 修改 `demo终.py`，Streamlit 页面只加载预训练模型和缓存特征，不再在页面交互时临时训练。
- 解决页面首次打开慢、交互卡顿、滑块变化触发重算的问题。

### 4. 数据读取与试次对齐

- 新增 `eeg_pipeline.py`，统一处理 CSV 读取、试次分组、ICA、特征提取和缓存。
- 使用 CSV 中的 `epoch` 列进行试次对齐，不再按固定 3 秒硬切。
- 修复真实数据每个 epoch 为 201 个采样点、而不是 750 个采样点导致训练失败的问题。
- 自动采用最常见的 epoch 长度，并忽略被 `nrows` 截断造成的尾部不完整试次。
- 禁止伪标签循环拼接，避免数据泄漏和标签错配。

### 5. ICA 与特征缓存

- ICA 去噪不再空转，固定剔除 `[0, 1]` 成分。
- 训练脚本会把预处理后的 EEG 片段、特征矩阵、频谱数据保存到 `artifacts/features_<mode>.joblib`。
- Streamlit 页面直接读取缓存产物，提升演示稳定性。

### 6. 模型评估与数据泄漏控制

- 训练脚本优先按 `patient` 做分组划分，减少同一受试者数据同时进入训练集和测试集造成的泄漏。
- 如果分组划分条件不足，则回退到标签分层划分。
- 生成 JSON 和 Markdown 两种评估报告。

### 7. 演示模式与完整模式

- 页面新增运行模式：
  - `演示模式`：加载轻量训练产物，适合现场展示。
  - `完整数据模式`：加载全量训练产物，适合正式评估。
- 两种模式分别对应不同模型和缓存文件，互不干扰。

### 8. 指令映射显示修复

- 修复简单模式里“切换指令映射后，看起来识别结果跟着变”的问题。
- 现在结果卡片明确拆成两部分：
  - `脑电识别意图`：模型对 EEG 的识别结果。
  - `映射后的执行指令`：根据当前映射表转换出的设备动作。
- 说明：切换映射只改变执行指令，不改变脑电识别意图。

### 9. JSON 输出顺序修复

- 修复 `latest_prediction.json` 在指令映射更新前就写入的问题。
- 现在先完成模型预测和指令映射，再写入结构化 JSON。
- JSON 中新增：
  - `prediction`
  - `command_mapping`
  - `runtime_security`

### 10. 风险分数与安全状态修复

- 修复简单模式不更新安全系统的问题。
- 每次识别都会记录运行事件，更新：
  - 当前频率
  - 历史指令
  - 审计日志
  - 风险状态机
- 安全测试改为临时安全系统执行，不再污染正常演示风险分数。
- 新增 `重置风险状态` 按钮，可清空风险状态机、审计日志、序列分析和限流状态。
- 风险分数下方新增 `风险因子` 展开项，显示分数来源。

### 11. 安全状态显示体验

- 修复风险分数可能一直 9/10 或一直 0/10 的体验问题。
- 简单模式下也会更新当前频率和历史指令。
- 结果卡片下方会立即显示本次更新后的运行安全指标。
- 侧边栏指标会在下一次页面刷新或交互时同步。

## 二、目录说明

```text
C4/
├─ demo终.py                         Streamlit 演示入口
├─ train.py                          离线训练脚本
├─ eeg_pipeline.py                   EEG 数据处理与特征缓存管道
├─ requirements.txt                  Python 依赖
├─ README.md                         简版运行说明
├─ 优化清单与使用说明.md              本文档
├─ BCICIV_2a_all_patients.csv         主数据集
├─ data/
│  └─ demo_patients.csv               备用/演示数据
├─ models/
│  └─ <mode>/                         训练后的模型文件
├─ artifacts/
│  └─ features_<mode>.joblib          特征缓存
└─ reports/
   └─ model_evaluation_<mode>.*       评估报告
```

## 三、安装依赖

在 PowerShell 中执行：

```powershell
Set-Location "C:\Users\dongsuo\Desktop\C4"
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

如果 `python` 指向不对，可以改用：

```powershell
py -m pip install -r requirements.txt
```

## 四、训练模型

### 1. 训练演示模式

推荐先运行：

```powershell
python train.py --mode demo
```

演示模式会生成：

```text
models/demo/eeg_intent_model.pkl
models/demo/eeg_scaler.pkl
models/demo/eeg_label_encoder.pkl
models/demo/training_metadata.json
artifacts/features_demo.joblib
reports/model_evaluation_demo.json
reports/model_evaluation_demo.md
```

### 2. 快速排查训练链路

如果想先确认训练链路是否通，可以跳过 ICA：

```powershell
python train.py --mode demo --skip-ica
```

### 3. 训练完整模式

完整数据训练：

```powershell
python train.py --mode full
```

如果完整模式太慢，可以先做有限样本 smoke test：

```powershell
python train.py --mode full --max-epochs 128 --max-rows 384000
```

## 五、启动演示系统

训练完 `demo` 模式后运行：

```powershell
python -m streamlit run "demo终.py"
```

页面打开后，在侧边栏选择：

- `演示模式`：使用 `models/demo/` 和 `artifacts/features_demo.joblib`
- `完整数据模式`：使用 `models/full/` 和 `artifacts/features_full.joblib`

注意：选择某个模式前，必须先用 `train.py --mode <mode>` 生成对应产物。

## 六、页面使用说明

### 1. 选择运行模式

在左侧栏选择：

- `演示模式`：推荐现场展示使用。
- `完整数据模式`：用于完整数据评估。

### 2. 选择试次

在主界面左侧通过滑块选择试次编号。

中间区域会显示：

- EEG 波形
- 功率谱图

### 3. 选择指令映射模式

#### 简单模式

简单模式用于演示“意图到指令”的基础映射。

例如：

```text
左手运动 → 轮椅左拐
右手运动 → 轮椅右拐
双脚运动 → 轮椅前进
舌头运动 → 轮椅后退
```

模型识别的是脑电意图，指令映射只是把识别出的意图转换成设备动作。

#### 高级模式

高级模式会进行结构化指令映射和安全过滤。

会检查：

- 置信度是否达标
- 指令是否在白名单内
- 意图和指令是否一致
- 是否包含危险关键字
- 参数是否安全
- 是否存在频率攻击
- 是否存在重复指令模式
- 是否受夜间情境限制
- 当前系统状态是否允许执行

### 4. 点击识别

点击：

```text
开始识别与安全验证
```

页面会显示：

- 脑电识别意图
- 映射后的执行指令
- 置信度
- 指令风险等级
- 系统状态
- 风险分数
- 置信度柱状图
- 本次更新后的当前频率、历史指令和风险分数

## 七、风险分数说明

风险分数不是模型置信度，也不是脑电信号本身的分数。

风险分数来自安全状态机，主要由以下因素组成：

- 最近 10 条安全事件中的拦截率，最高 5 分。
- 夜间时段运行，加 2 分。
- 最近连续 3 次高危事件，加 4 分。

如果风险分数异常偏高，可以点击侧边栏：

```text
重置风险状态
```

该按钮会清空：

- 风险状态机
- 审计日志
- 行为序列历史
- 频率限制状态

## 八、常见问题

### 1. `No complete EEG epochs were found`

这个问题已修复。原因是原先假设每个试次 750 个采样点，但真实 CSV 的每个 epoch 是 201 个采样点。

现在系统会自动按 CSV 的真实 epoch 长度处理。

### 2. 页面提示缺少特征缓存

说明还没有训练对应模式。

运行：

```powershell
python train.py --mode demo
```

或：

```powershell
python train.py --mode full
```

### 3. 简单模式切换映射后结果变化

这是正常的映射变化，不是脑电识别意图变化。

现在页面已把两者拆开：

- 脑电识别意图
- 映射后的执行指令

### 4. 当前频率和历史指令不变

这个问题已修复。简单模式现在也会记录运行事件。

如果侧边栏没有立刻变化，看结果卡片下方的“本次更新后”指标；侧边栏会在下一次刷新或交互时同步。

### 5. 风险分数一直 9/10

通常是安全测试或连续拦截日志把风险状态推高。

现在安全测试不会污染正常演示状态。如果仍然偏高，点击：

```text
重置风险状态
```

## 九、推荐演示流程

1. 安装依赖。
2. 执行 `python train.py --mode demo`。
3. 执行 `python -m streamlit run "demo终.py"`。
4. 选择 `演示模式`。
5. 选择一个试次。
6. 使用简单模式展示“脑电意图 → 执行指令”。
7. 切换简单模式映射，说明识别意图不变，执行指令变化。
8. 切换高级模式，展示结构化指令和安全过滤。
9. 运行安全测试，展示系统防护能力。
10. 如风险分数偏高，点击 `重置风险状态` 后继续演示。
